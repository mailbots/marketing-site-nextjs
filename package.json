{
  "name": "mailbots-landing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "rm -fr out && NODE_ENV=production next build && NODE_ENV=production next export && cp now.json out && now out && now alias"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@zeit/next-css": "^1.0.1",
    "@zeit/next-sass": "^1.0.1",
    "next": "^7.0.2",
    "next-fonts": "^0.17.0",
    "next-images": "^1.0.4",
    "normalize.css": "^8.0.1",
    "react": "^16.8.0-alpha.1",
    "react-dom": "^16.8.0-alpha.1"
  },
  "devDependencies": {
    "node-sass": "^4.11.0"
  }
}
