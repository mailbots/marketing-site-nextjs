{
  "name": "mailbots-landing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "next",
    "dev:server": "cd server && npm run dev",
    "build": "rm -fr ./out && rm -fr ./server/dist && NODE_ENV=production next build && NODE_ENV=production next export && cp -r ./out ./server/dist",
    "start": "next start",
    "static:local": "cd server && npm run dev",
    "deploy": "npm run build && cd server && now && now alias"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@zeit/next-css": "^1.0.1",
    "@zeit/next-sass": "^1.0.1",
    "axios": "^0.18.0",
    "next": "^7.0.2",
    "next-fonts": "^0.17.0",
    "next-images": "^1.0.4",
    "normalize.css": "^8.0.1",
    "nprogress": "^0.2.0",
    "react": "^16.8.0-alpha.1",
    "react-dom": "^16.8.0-alpha.1",
    "react-toastify": "^4.5.2"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "node-sass": "^4.11.0"
  },
  "proxy": "http://localhost:3002"
}
